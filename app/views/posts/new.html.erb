    <!-- キービジュアル -->
    <%= image_tag "key_visual_pop.jpg", :class => "cover" %>
    <p id="bar">Answer.me</p>
    <%= image_tag "about_answer.png", :class => "about_answer", :id => "animation" %>
    <%= image_tag "how_to_answer.png", :class => "how_to_answer", :id => "animation2" %>

  <script type="text/javascript">
  $('#animation').css('visibility','hidden');
  $(window).scroll(function(){
   var windowHeight = $(window).height(),
       topWindow = $(window).scrollTop();
   $('#animation').each(function(){
    var targetPosition = $(this).offset().top;
    if(topWindow > targetPosition - windowHeight + 100){
     $(this).addClass("fadeInDown");
    }
   });
  });

  $('#animation2').css('visibility','hidden');
  $(window).scroll(function(){
   var windowHeight = $(window).height(),
       topWindow = $(window).scrollTop();
   $('#animation2').each(function(){
    var targetPosition = $(this).offset().top;
    if(topWindow > targetPosition - windowHeight + 100){
     $(this).addClass("fadeInDown");
    }
   });
  });
  </script>



<!-- 部分テンプレート -->
<%= render 'form', post: @post %>
 <!-- render 部分テンプレート名, キー１:値１
 アクション内で定義した@post変数に、form部分テンプレート内でpost変数でアクセスできる -->
